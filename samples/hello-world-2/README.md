# Hello, World 2 (Paired layout) with BACON

This sample generates a dataset from a random paired-structure Boolean expression, and trains BACON with a paired tree layout to recover that expression.

## Run the code
```bash
python main.py
```

The model uses the `paired` layout and the `bool.min_max` aggregator.


